#include <iostream>
using namespace std;

int main()
{
    /*
    int airportAmount = 0;
    cin >> airportAmount;
    int** airlinePrice = new int*[airportAmount];
    for(int i = 0; i < airportAmount; i++)
    {
        airlinePrice[i] = new int[2];
        cin >> airlinePrice[i][0] >> airlinePrice[i][1];
    }
    for(int i = 0; i < airportAmount; i++)
    {
        for(int j = 0; j < 2; j++)
            cout << airlinePrice[i][j] << " ";
        cout << endl;
    }*/
    return 0;
}